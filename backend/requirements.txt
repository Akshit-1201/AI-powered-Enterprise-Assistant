# --- Web / API ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# --- Persistence ---
SQLAlchemy>=2.0

# --- Orchestration / LLM ---
langgraph>=0.2.28
langgraph-checkpoint-sqlite>=1.0.0
langchain-core>=0.2.40
langchain-openai>=0.1.20

# --- RAG / documents (Phase 2) ---
chromadb>=0.5.0
langchain-text-splitters>=0.2.0
pypdf>=4.0
python-multipart>=0.0.9

# --- Auth (Phase 3) ---
python-jose[cryptography]>=3.3
bcrypt>=4.0          # used directly; passlib 1.7.4 is unmaintained & breaks on bcrypt 5.x
email-validator>=2.0

# --- Testing ---
pytest>=8.0
httpx>=0.27
